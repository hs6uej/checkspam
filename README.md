# 🤖 SMS Screening AI System (Next.js + Gemini)

## บทนำ (Introduction)

โปรเจกต์นี้คือระบบเว็บแอปพลิเคชันที่สร้างด้วย **Next.js** และ **React Bootstrap** เพื่อใช้คัดกรองข้อความ SMS ที่ผู้ใช้อัปโหลด (ในรูปแบบ CSV หรือ Excel) โดยใช้ **Google Gemini API** ในการวิเคราะห์และจัดประเภทข้อความว่าเป็น `pass` (ข้อความปกติ/ธุรกรรม) หรือ `not pass` (สแปม/หลอกลวง)

### คุณสมบัติหลัก

* **File Upload:** รองรับไฟล์ CSV และ Excel (.xlsx)
* **Progress Bar:** แสดงแถบความคืบหน้าแบบ Real-time ขณะประมวลผลด้วย AI
* **Data Preview (Enhanced):** แสดงข้อมูลที่อัปโหลดทั้งหมดในรูปแบบ **DataTables** ที่สามารถค้นหาและจัดเรียงได้ ก่อนเริ่มการคัดกรอง
* **Gemini AI Screening:** ใช้ `gemini-2.5-flash` สำหรับการวิเคราะห์บริบท
* **Interactive Results:** แสดงผลลัพธ์สุดท้ายด้วย **DataTables** พร้อมฟังก์ชันค้นหา/จัดเรียง และไฮไลท์สีตามสถานะ (`pass`/`not pass`)
* **Dedicated Download Section:** แยกปุ่มดาวน์โหลด CSV และ Excel ออกมาเป็นหัวข้อที่ 4 เพื่อความชัดเจน

---

## 🛠️ การติดตั้งและการตั้งค่า (Setup)

โปรเจกต์นี้ใช้ **Node.js**, **Next.js** และ **Yarn** ในการจัดการแพ็กเกจ

### ข้อกำหนดเบื้องต้น

* Node.js (เวอร์ชัน 18+)
* Yarn Package Manager
* Google Gemini API Key

### ขั้นตอนการติดตั้ง

1.  **Clone หรือดาวน์โหลดโค้ด:**
    ```bash
    git clone [https://medium.com/@5735512017/%E0%B8%AA%E0%B8%A3%E0%B8%B8%E0%B8%9B-github-%E0%B8%97%E0%B8%B5%E0%B9%88%E0%B8%88%E0%B8%B3%E0%B9%80%E0%B8%9B%E0%B9%87%E0%B8%99%E0%B8%95%E0%B9%89%E0%B8%AD%E0%B8%87%E0%B8%A3%E0%B8%B9%E0%B9%89-fd0df64cada0](https://medium.com/@5735512017/%E0%B8%AA%E0%B8%A3%E0%B8%B8%E0%B8%9B-github-%E0%B8%97%E0%B8%B5%E0%B9%88%E0%B8%88%E0%B8%B3%E0%B9%80%E0%B8%9B%E0%B9%87%E0%B8%99%E0%B8%95%E0%B9%89%E0%B8%AD%E0%B8%87%E0%B8%A3%E0%B8%B9%E0%B9%89-fd0df64cada0)
    cd CheckSpam
    ```

2.  **ติดตั้ง Dependencies ทั้งหมด:**
    รันคำสั่งนี้เพื่อติดตั้ง Next.js, Bootstrap, Gemini SDK, DataTables และไลบรารีสำหรับจัดการไฟล์:
    ```bash
    yarn add next react react-dom react-bootstrap bootstrap sweetalert2 formidable csv-parser xlsx @google/genai jquery datatables.net datatables.net-dt datatables.net-bs5
    ```

3.  **ตั้งค่า Gemini API Key:**
    สร้างไฟล์ชื่อ `.env.local` ในรูทของโปรเจกต์ และใส่ Gemini API Key ของคุณลงไป:
    ```
    # .env.local
    GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"
    ```
    ⚠️ **คำเตือนด้านความปลอดภัย:** ไฟล์ `.env.local` ถูกยกเว้นจาก Git โดย `.gitignore`

---

## 🚀 วิธีการใช้งาน (Usage)

1.  **รันโปรเจกต์:**
    ```bash
    yarn dev
    ```
    แอปพลิเคชันจะเปิดที่: `http://localhost:3000`

2.  **การเตรียมไฟล์ข้อมูล:**
    ไฟล์ CSV หรือ Excel ที่อัปโหลด **ต้องมี** 2 คอลัมน์หลัก คือ `sender` และ `text`

3.  **ขั้นตอนการใช้งานในแอป:**
    * **1. อัปโหลดไฟล์:** เลือกไฟล์ CSV/Excel
    * **Data Preview:** ตรวจสอบข้อมูลทั้งหมดที่อัปโหลดในตาราง **DataTables**
    * **2. เริ่มประมวลผล:** กดปุ่ม **"🚀 เริ่มประมวลผลด้วย Gemini AI"** และรอจนกว่า Progress Bar จะเสร็จสิ้น
    * **3. ผลลัพธ์การคัดกรอง:** ดูผลลัพธ์ในตารางที่มีสีไฮไลท์
    * **4. ดาวน์โหลดผลลัพธ์:** ใช้ปุ่ม **"Download CSV"** หรือ **"Download Excel"** ที่อยู่ด้านล่างตารางเพื่อบันทึกผลลัพธ์

---

## ⚙️ โครงสร้างโปรเจกต์ (Project Structure)

| ไฟล์/โฟลเดอร์ | หน้าที่ |
| :--- | :--- |
| `pages/index.js` | **Frontend (UI):** หน้าหลัก, UI (Bootstrap), DataTables, Logic การเรียก API และ Progress Bar |
| `pages/api/upload-and-read.js` | **Backend API:** รับไฟล์ที่อัปโหลด, อ่านข้อมูล CSV/Excel (ไม่มี AI Call) |
| `pages/api/process-one.js` | **Backend API:** รับข้อความ 1 ข้อความ, เรียกใช้ **Gemini API** เพื่อวิเคราะห์ (AI Call) |
| `pages/_document.js` | **Next.js Config:** จัดการการโหลด Global CSS/Icons (เช่น Bootstrap Icons) |
| `.env.local` | จัดเก็บ **GEMINI_API_KEY** อย่างปลอดภัย |

---

*พัฒนาโดย AI Assistant ด้วย Next.js และ Gemini API*